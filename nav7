index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>ShopEasy Navigation</title>
  <base href="/">
</head>
<body>
  <app-root></app-root>
</body>
</html>


app.html
<!-- Navbar -->
<div class="navbar">
  <h1 class="logo">🛍️ ShopEasy</h1>
  <div class="menu">
    <button (click)="navigate('home')">Home</button>
    <button (click)="navigate('products')">Products</button>
    <button (click)="navigate('cart')">Cart ({{ cart.length }})</button>
  </div>
</div>

<!-- Main Content -->
<div class="content">

  <!-- Home Page -->
  <div *ngIf="currentPage === 'home'" class="page">
    <h2>Welcome to ShopEasy!</h2>
    <p>Discover amazing products at the best prices.</p>
    <img src="https://img.freepik.com/free-vector/shopping-online-concept-illustration_114360-1084.jpg"
         alt="Shopping Banner" class="banner">
  </div>

  <!-- Products Page -->
  <div *ngIf="currentPage === 'products'" class="page">
    <h2>Our Products</h2>
    <div class="product-grid">
      <div class="card" *ngFor="let p of products">
        <img [src]="p.image" [alt]="p.name">
        <h3>{{ p.name }}</h3>
        <p>₹{{ p.price }}</p>
        <button (click)="addToCart(p)">Add to Cart</button>
      </div>
    </div>
  </div>

  <!-- Cart Page -->
  <div *ngIf="currentPage === 'cart'" class="page">
    <h2>Your Shopping Cart</h2>

    <div *ngIf="cart.length === 0">
      <p>Your cart is empty!</p>
      <img src="https://img.freepik.com/free-vector/empty-cart-concept-illustration_114360-2155.jpg"
           alt="Empty Cart" class="banner">
    </div>

    <div *ngIf="cart.length > 0">
      <ul class="cart-list">
        <li *ngFor="let item of cart; let i = index">
          <span>{{ item.name }} - ₹{{ item.price }}</span>
          <button class="remove-btn" (click)="removeFromCart(i)">❌ Remove</button>
        </li>
      </ul>
      <h3>Total: ₹{{ getTotal() }}</h3>
    </div>
  </div>
</div>

app.css
/* Navbar */
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(to right, #ff9800, #e65100);
  padding: 15px 30px;
  color: white;
}

.logo {
  font-size: 1.5rem;
  font-weight: bold;
}

.menu button {
  margin-left: 15px;
  padding: 8px 15px;
  border: none;
  background: white;
  color: #e65100;
  font-weight: bold;
  border-radius: 6px;
  cursor: pointer;
  transition: 0.3s;
}

.menu button:hover {
  background: #ffe0b2;
}

/* Main Content */
.content {
  padding: 20px;
  text-align: center;
}

.page {
  margin-top: 20px;
}

.banner {
  margin-top: 20px;
  max-width: 400px;
  border-radius: 12px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);
}

/* Product Grid */
.product-grid {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-top: 20px;
}

.card {
  background: #fff3e0;
  padding: 15px;
  border-radius: 12px;
  width: 200px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.15);
  transition: transform 0.2s;
}

.card:hover {
  transform: translateY(-5px);
}

.card img {
  width: 100%;
  height: 120px;
  object-fit: cover;
  border-radius: 8px;
}

.card button {
  margin-top: 10px;
  padding: 8px 12px;
  border: none;
  background: #e65100;
  color: white;
  border-radius: 5px;
  cursor: pointer;
  transition: 0.2s;
}

.card button:hover {
  background: #bf360c;
}

/* Cart List */
.cart-list {
  list-style: none;
  padding: 0;
}

.cart-list li {
  background: #fff8e1;
  margin: 8px auto;
  padding: 10px 15px;
  border-radius: 8px;
  max-width: 400px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.remove-btn {
  background: #d32f2f;
  color: white;
  border: none;
  padding: 5px 10px;
  border-radius: 5px;
  cursor: pointer;
  transition: 0.2s;
}

.remove-btn:hover {
  background: #b71c1c;
}

main.ts
import 'zone.js';   // <--- required

import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app';

bootstrapApplication(AppComponent)
  .catch(err => console.error(err));

app.ts
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

interface Product {
  name: string;
  price: number;
  image: string;
}

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './app.html',
  styleUrls: ['./app.css']
})
export class AppComponent {
  currentPage: string = 'home';

  // Sample products
  products: Product[] = [
    { name: 'Laptop', price: 50000, image: 'https://img.freepik.com/free-photo/laptop_74190-2283.jpg' },
    { name: 'Smartphone', price: 20000, image: 'https://img.freepik.com/free-photo/smartphone_144627-31048.jpg' },
    { name: 'Headphones', price: 2000, image: 'https://img.freepik.com/free-photo/headphones_144627-14864.jpg' }
  ];

  // Cart items
  cart: Product[] = [];

  navigate(page: string) {
    this.currentPage = page;
  }

  addToCart(product: Product) {
    this.cart.push(product);
    alert(`${product.name} added to cart!`);
  }

  removeFromCart(index: number) {
    this.cart.splice(index, 1);
  }

  getTotal(): number {
    return this.cart.reduce((sum, item) => sum + item.price, 0);
  }
}


app.routes.ts
import { Routes } from '@angular/router';

export const routes: Routes = [];
